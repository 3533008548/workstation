# 知识蒸馏

## 基本形式

知识蒸馏（knowledge distillation）用大模型（教师）的软标签训练小模型（学生）。
软标签携带类间相似性，比硬标签信息更丰富，因此学生能在更少参数下逼近教师精度。

## 毕业设计中的方案

我的实验用温度 T=4 的软标签 + 硬标签联合损失，学生网络为 3 层 CNN。在 CIFAR-100 上
学生达到教师 95% 的精度，推理时延下降 3.2 倍，适合边缘设备部署。

## 与量化的对比

蒸馏保留结构信息，量化只压缩数值精度。两者可叠加：先蒸馏再 INT8 量化，进一步压缩
而不明显掉点。
