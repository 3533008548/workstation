"""技能适配器 —— 把既有项目的原生能力接到工作台契约上。

每个子包负责一个既有项目，逐个替换掉"直接消费三方格式"的做法：

    ppt/         PPTAgent（Node，一次性子进程）
    （阶段 3）   知识库桌面端（HTTP 长驻服务）

规矩只有一条：**底座不直接消费三方格式**。适配器把三方格式逐字段映射成
工作台契约，三方多出一个字段，错误应该在这里炸，而不是在上层静默错。
"""

from __future__ import annotations

__all__: list[str] = []
